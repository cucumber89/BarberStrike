/** The map as built in `packages/shared/src/dolna.ts`, in the harness's shape — so the port is measured, not assumed. */
import { DOLNA, DOLNA_PLACES, DOLNA_EXITS, DOLNA_GROUND, DOLNA_BOUNDARY } from "../../../../../packages/shared/src/dolna";
export const MAP = DOLNA;
export const PLACES = DOLNA_PLACES;
export const EXITS = DOLNA_EXITS;
export const GROUND = DOLNA_GROUND;
export const BOUNDARY = DOLNA_BOUNDARY;
